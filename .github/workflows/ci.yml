name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: python -m pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check python/

      - name: Type check with mypy
        run: mypy python/hapticore/core/ --strict

      - name: Run unit tests
        run: pytest tests/unit/ -v

      - name: Run integration tests
        run: pytest tests/integration/ -v

  test-macos:
    name: Test (macOS, Python 3.12)
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: python -m pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check python/

      - name: Type check with mypy
        run: mypy python/hapticore/core/ --strict

      - name: Run unit tests
        run: pytest tests/unit/ -v

      - name: Run integration tests
        run: pytest tests/integration/ -v --timeout=120

  test-cpp:
    name: C++ haptic_server tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Install Ninja (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y ninja-build libzmq3-dev

      - name: Install Ninja (macOS)
        if: runner.os == 'macOS'
        run: brew install ninja zeromq

      - name: Configure
        run: cmake --preset ci
        working-directory: cpp/haptic_server

      - name: Build
        run: cmake --build --preset ci
        working-directory: cpp/haptic_server

      - name: Test
        run: ctest --preset ci
        working-directory: cpp/haptic_server

      - name: Smoke test
        run: ./build/ci/haptic_server --help
        working-directory: cpp/haptic_server

  test-cpp-tsan:
    name: C++ ThreadSanitizer
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Install Ninja
        run: sudo apt-get update && sudo apt-get install -y ninja-build

      - name: Install libzmq
        run: sudo apt-get install -y libzmq3-dev

      - name: Configure
        run: cmake --preset tsan
        working-directory: cpp/haptic_server

      - name: Build
        run: cmake --build --preset tsan
        working-directory: cpp/haptic_server

      - name: Test
        run: ctest --preset tsan
        working-directory: cpp/haptic_server

  test-cross-language:
    name: Cross-language serialization
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y ninja-build libzmq3-dev

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install Python package
        run: python -m pip install -e ".[dev]"

      - name: Build C++ (mock)
        run: |
          cmake --preset ci
          cmake --build --preset ci
        working-directory: cpp/haptic_server

      - name: Run cross-language test
        run: pytest tests/integration/test_cross_language.py -v
